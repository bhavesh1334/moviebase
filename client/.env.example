# Frontend environment variables (public)
# Used in next.config.ts to rewrite /api → <API_URL>/api/v1
# Replace with your deployed backend URL when not running locally
NEXT_PUBLIC_API_URL=http://localhost:3000
